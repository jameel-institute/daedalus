# Test if openness is passed correctly

Test if openness is passed correctly

## Usage

``` r
test_openness(x, settings)
```

## Arguments

- x:

  A numeric vector or matrix to be validated as an accepted way of
  specifying contacts scaling.

- settings:

  A number for the number of settings
